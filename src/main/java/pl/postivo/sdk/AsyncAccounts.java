/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package pl.postivo.sdk;

import static pl.postivo.sdk.operations.Operations.AsyncRequestlessOperation;
import static pl.postivo.sdk.operations.Operations.AsyncRequestOperation;

import jakarta.annotation.Nonnull;
import jakarta.annotation.Nullable;
import java.lang.String;
import java.util.concurrent.CompletableFuture;
import pl.postivo.sdk.models.operations.GetSubaccountDetailsRequest;
import pl.postivo.sdk.models.operations.async.GetAccountDetailsRequestBuilder;
import pl.postivo.sdk.models.operations.async.GetAccountDetailsResponse;
import pl.postivo.sdk.models.operations.async.GetSubaccountDetailsRequestBuilder;
import pl.postivo.sdk.models.operations.async.GetSubaccountDetailsResponse;
import pl.postivo.sdk.operations.GetAccountDetails;
import pl.postivo.sdk.operations.GetSubaccountDetails;
import pl.postivo.sdk.utils.Headers;
import pl.postivo.sdk.utils.Options;


public class AsyncAccounts {
    private static final Headers _headers = Headers.EMPTY;
    private final SDKConfiguration sdkConfiguration;
    private final Accounts syncSDK;

    AsyncAccounts(Accounts syncSDK, SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
        this.syncSDK = syncSDK;
    }

    /**
     * Switches to the sync SDK.
     * 
     * @return The sync SDK
     */
    public Accounts sync() {
        return syncSDK;
    }


    /**
     * Retrieve account details
     * 
     * <p>Retrieve the current account balance and other account details. You can also check the account limit
     * and whether the account is a **main** account. Main accounts have unrestricted privileges and, via
     * the [User Panel](https://panel.postivo.pl), you can create as many subaccounts as needed.
     * 
     * @return The async call builder
     */
    public GetAccountDetailsRequestBuilder get() {
        return new GetAccountDetailsRequestBuilder(sdkConfiguration);
    }

    /**
     * Retrieve account details
     * 
     * <p>Retrieve the current account balance and other account details. You can also check the account limit
     * and whether the account is a **main** account. Main accounts have unrestricted privileges and, via
     * the [User Panel](https://panel.postivo.pl), you can create as many subaccounts as needed.
     * 
     * @return {@code CompletableFuture<GetAccountDetailsResponse>} - The async response
     */
    public CompletableFuture<GetAccountDetailsResponse> getDirect() {
        return get(null);
    }

    /**
     * Retrieve account details
     * 
     * <p>Retrieve the current account balance and other account details. You can also check the account limit
     * and whether the account is a **main** account. Main accounts have unrestricted privileges and, via
     * the [User Panel](https://panel.postivo.pl), you can create as many subaccounts as needed.
     * 
     * @param options additional options
     * @return {@code CompletableFuture<GetAccountDetailsResponse>} - The async response
     */
    public CompletableFuture<GetAccountDetailsResponse> get(@Nullable Options options) {
        AsyncRequestlessOperation<GetAccountDetailsResponse> operation
            = new GetAccountDetails.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest()
            .thenCompose(operation::handleResponse);
    }


    /**
     * Get subaccount details
     * 
     * <p>Check the account balance and other details, such as the subcredit balance of a subaccount.
     * Subaccounts are additional users who can access your account’s services and data. You can restrict
     * access levels and set privileges for subaccounts in the [User Panel](https://panel.postivo.pl).
     * 
     * <p>Provide the full subaccount login to access its data.
     * 
     * @return The async call builder
     */
    public GetSubaccountDetailsRequestBuilder getSubaccount() {
        return new GetSubaccountDetailsRequestBuilder(sdkConfiguration);
    }

    /**
     * Get subaccount details
     * 
     * <p>Check the account balance and other details, such as the subcredit balance of a subaccount.
     * Subaccounts are additional users who can access your account’s services and data. You can restrict
     * access levels and set privileges for subaccounts in the [User Panel](https://panel.postivo.pl).
     * 
     * <p>Provide the full subaccount login to access its data.
     * 
     * @param userLogin Login of the subaccount (user) for which to retrieve data.
     * @return {@code CompletableFuture<GetSubaccountDetailsResponse>} - The async response
     */
    public CompletableFuture<GetSubaccountDetailsResponse> getSubaccount(@Nonnull String userLogin) {
        return getSubaccount(userLogin, null);
    }

    /**
     * Get subaccount details
     * 
     * <p>Check the account balance and other details, such as the subcredit balance of a subaccount.
     * Subaccounts are additional users who can access your account’s services and data. You can restrict
     * access levels and set privileges for subaccounts in the [User Panel](https://panel.postivo.pl).
     * 
     * <p>Provide the full subaccount login to access its data.
     * 
     * @param userLogin Login of the subaccount (user) for which to retrieve data.
     * @param options additional options
     * @return {@code CompletableFuture<GetSubaccountDetailsResponse>} - The async response
     */
    public CompletableFuture<GetSubaccountDetailsResponse> getSubaccount(@Nonnull String userLogin, @Nullable Options options) {
        GetSubaccountDetailsRequest request = new GetSubaccountDetailsRequest(userLogin);
        AsyncRequestOperation<GetSubaccountDetailsRequest, GetSubaccountDetailsResponse> operation
              = new GetSubaccountDetails.Async(
                                    sdkConfiguration, options, sdkConfiguration.retryScheduler(),
                                    _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }

}
